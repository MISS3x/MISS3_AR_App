// Auto-generated 16x16 transparent PNG with a white cross in the middle.
// Encoded directly into JS to avoid Metro require() bundler issues and React Native runtime asset resolution caches.

export const crossTexture = require('../../assets/cross_pattern.png');

// 64x64 transparent PNG with a white ring (O) in the middle.
export const ringTexture = require('../../assets/ring_pattern.png');
